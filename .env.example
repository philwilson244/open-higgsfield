HF_API_BASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Public origin for email confirmations, e.g. https://ads.example.com
APP_URL=
# 32 random bytes encoded as 64 hex characters; server only, never NEXT_PUBLIC_.
# Keep this stable and backed up. Changing it requires re-entering provider keys.
PROVIDER_ENCRYPTION_KEY=
